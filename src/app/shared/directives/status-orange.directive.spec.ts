import { StatusOrangeDirective } from './status-orange.directive';

describe('StatusOrangeDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusOrangeDirective();
    expect(directive).toBeTruthy();
  });
});
