import { StatusRedDirective } from './status-red.directive';

describe('StatusRedDirective', () => {
  it('should create an instance', () => {
    const directive = new StatusRedDirective();
    expect(directive).toBeTruthy();
  });
});
