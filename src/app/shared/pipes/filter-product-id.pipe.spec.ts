import { FilterProductIdPipe } from './filter-product-id.pipe';

describe('FilterProductIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProductIdPipe();
    expect(pipe).toBeTruthy();
  });
});
