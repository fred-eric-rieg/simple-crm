import { FilterTaskIdPipe } from './filter-task-id.pipe';

describe('FilterTaskIdPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTaskIdPipe();
    expect(pipe).toBeTruthy();
  });
});
